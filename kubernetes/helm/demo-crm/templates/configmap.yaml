apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-crm.fullname" . }}-config
  labels:
    {{- include "demo-crm.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
